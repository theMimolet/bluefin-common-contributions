name: Run Testsuite

# Reusable wrapper around projectbluefin/testsuite e2e.yml.
# Keeps the testsuite SHA pin in one place for PR, post-merge, and
# promotion-candidate gates in this repo.
on:
  workflow_call:
    inputs:
      image:
        description: "Full image ref to test"
        required: true
        type: string
      suites:
        description: "Comma-separated test suites to run"
        required: true
        type: string

permissions:
  contents: read
  packages: write

jobs:
  e2e:
<<<<<<< HEAD
    uses: projectbluefin/testsuite/.github/workflows/e2e.yml@20fb066e50c81d62d15cf84633d36c260c4115db # main
=======
    uses: projectbluefin/testsuite/.github/workflows/e2e.yml@20fb066e50c81d62d15cf84633d36c260c4115db # main
>>>>>>> 7340aa0 (ci: bump testsuite SHA to 20fb066 (all env + screenshot fixes))
    permissions:
      contents: read
      packages: write
    with:
      image: ${{ inputs.image }}
      suites: ${{ inputs.suites }}
